#include "main.h"
/**
 * add - adds two numbers and returns the result
 * @x: first parameter
 * @y: second parameter
 * Return: returns sum
 */
int add(int x, int y)
{
	int z = (x + y);

	return (z);
}
