#include "main.h"

/**
 * mul - multiplies two integers
 * @a: interger param
 * @b: interger param
 * Return: returns product of two params
 */

int mul(int a, int b)
{
	int c;

	c = (a * b);
	return (c);
}
