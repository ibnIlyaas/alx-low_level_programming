#include "main.h"
#include <string.h>

/**
 * _strlen_recursion - returns lenght of a string
 * @s: String param
 * Return: length of string
 */

int _strlen_recursion(char *s)
{
	int len = strlen(s);
	return (len);
}
